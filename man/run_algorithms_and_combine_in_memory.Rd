% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_algorithms_and_combine.R
\name{run_algorithms_and_combine_in_memory}
\alias{run_algorithms_and_combine_in_memory}
\title{Run selected algorithms in-memory (no bucket writes) and combine}
\usage{
run_algorithms_and_combine_in_memory(algos, main_name, key = "person_id", ...)
}
\arguments{
\item{algos}{named list of algorithms/specs (see \code{run_algorithms_and_combine()} for the format)}

\item{main_name}{which element of \code{algos} is the main dataset to left-join onto}

\item{key}{join key (default \"person_id\")}

\item{...}{shared args passed to every algorithm (optional)}
}
\value{
data.table
}
\description{
Same \code{algos} spec format as \code{run_algorithms_and_combine()}, but each algorithm is
called WITHOUT \code{output_folder}. Every algorithm ultimately writes via \code{.write_to_bucket()},
which returns its result data.frame directly instead of writing a CSV when \code{output_folder}
is not supplied. Results are kept in memory, keyed by the \code{algos} list name, so running the
same underlying algorithm more than once (e.g. before/after date windows) just works -- there
is no shared output file to collide on.
}
